/**
 * Member routes.
 * Handles server member management, kicks, bans, and member queries.
 * Will be implemented in Phase 8.
 */
import { Router } from 'express';

export const memberRouter = Router();
