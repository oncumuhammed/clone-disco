/**
 * Server routes.
 * Handles Discord server (guild) creation, updates, deletion, and queries.
 * Will be implemented in Phase 4.
 */
import { Router } from 'express';

export const serverRouter = Router();
