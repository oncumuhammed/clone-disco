/**
 * Authentication routes.
 * Handles user authentication, registration, login, and OAuth callbacks.
 * Will be implemented in Phase 2.
 */
import { Router } from 'express';

export const authRouter = Router();
