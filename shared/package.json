/**
 * Shared package for Concord monorepo.
 * Contains TypeScript types, constants, and utilities shared between client and server.
 */
{
  "name": "@concord/shared",
  "version": "1.0.0",
  "description": "Shared types, constants, and utilities for Concord",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
